# fsapps-pfmg-angular-front

Angular frontend application for project PFMG.

## Prerequisites

- Node.js 18+ and npm
- Angular CLI 17+

## Getting Started

1. Install dependencies:
```bash
npm install
```

2. Start the development server:
```bash
npm start
```

The application will be available at `http://localhost:8888`

## Available Scripts

- `npm start` - Start development server
- `npm run build` - Build for production
- `npm test` - Run tests
- `npm run lint` - Lint the code

## Project Structure

```
src/
├── app/
│   ├── app.component.ts
│   ├── app.component.html
│   └── app.component.scss
├── assets/
├── index.html
├── main.ts
└── styles.scss
```

## Configuration

The application connects to the backend API. Update the API URL in your environment files.

## Project ID

This project uses the identifier: **PFMG**
